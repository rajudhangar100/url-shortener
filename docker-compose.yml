version: '3.8'
services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://dhangarraju2005_db_user:wL4HwIefCYz5oiZl@cluster0.p38tapq.mongodb.net/?appName=Cluster0
      - PORT=5000

